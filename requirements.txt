# GuideNav Python dependencies.
#
# PyTorch is NOT listed here because the correct wheel depends on your CUDA
# version. Install it first, matching your setup, e.g. for CUDA 12.8:
#
#   pip install torch==2.7.0 torchvision==0.22.0 \
#       --index-url https://download.pytorch.org/whl/cu128
#
# ROS2 packages (rclpy, cv_bridge, sensor_msgs, geometry_msgs, std_msgs) come
# from a ROS2 Humble installation, not from pip. Create the venv with
# `python3.10 -m venv --system-site-packages` so they remain importable.

numpy<2
opencv-python
pandas
pyyaml
scipy
scikit-learn  # topogen/gen_dinov3.py (keyframe selection)
matplotlib
tqdm
h5py
pillow

# reloc3r / croco backbone requirements
einops
huggingface-hub[torch]>=0.22
imagesize
